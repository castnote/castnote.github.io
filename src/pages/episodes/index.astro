---
import { getCollection } from 'astro:content';
import BaseLayout from '@layouts/BaseLayout.astro';
import EpisodeList from '@components/EpisodeList.astro';

const episodes = (await getCollection('episodes'))
  .filter((episode) => episode.data.rights_status !== 'local_only')
  .sort((a, b) => b.data.published_at.getTime() - a.data.published_at.getTime());
---

<BaseLayout title="文章" description="听阅全部播客文章列表，一篇文章对应一期节目。">
  <section class="section-head reader-index-head">
    <div>
      <h1>文章</h1>
      <p class="lede">一篇文章对应一期节目。</p>
    </div>
  </section>

  <div class="reader-index">
    <EpisodeList episodes={episodes} />
  </div>
</BaseLayout>
